//変数の宣言(体重)
let bodyweight;
//値の代入(体重)
bodyweight = 68;

//変数の宣言(身長)
let height;
//値の代入(身長）
height = 1.7;


// 変数の宣言（BMI計算式)
let bmi;
// 値の代入（BMI計算式）
bmi = bodyweight / (height * height);

//コンソールへの出力
console.log(bmi)
